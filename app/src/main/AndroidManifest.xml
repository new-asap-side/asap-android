<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".components.AljyoApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Aljyo"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".components.onboarding.OnboardingActivity"
            android:exported="true"
            android:theme="@style/Theme.SplashScreen.Custom">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity
            android:name="com.kakao.sdk.auth.AuthCodeHandlerActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data android:host="oauth"
                    android:scheme="kakaod660673b614325c5987061bfd74d5393"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".components.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Aljyo" />

        <activity
            android:name=".components.group_details.GroupDetailsActivity"
            android:exported="true"
            android:theme="@style/Theme.Aljyo.Overlap"/>

    </application>

</manifest>